import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    reactStrictMode: true,
    images: {
        domains: ['wdpiwrdesqtlzsuxfkwc.supabase.co', 'images.unsplash.com'],
    },
};

export default nextConfig;
